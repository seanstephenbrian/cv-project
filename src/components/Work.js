import React from 'react';

import Add from './Add';

class Work extends React.Component {
    render() {
        return (
            <div>
                work
                <Add section="work" />
            </div>
        )
    }
}

export default Work;